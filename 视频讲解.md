# DataMind 智能数据分析平台 — 视频演示文字稿

> **时长预估**：15-18 分钟
> **演示数据集**：Online Retail II（UCI 电商数据集，约 50 万行 × 8 列）
> **对比演示**：可准备第二份非零售数据集（如鸢尾花、房价预测）展示普适性

---

## 第一章：服务启动与容器化部署（0:00 - 1:30）

### 画面：终端窗口

> 大家好，今天我来演示 DataMind 智能数据分析平台的完整功能。
>
> 首先看部署。DataMind 采用 Docker 容器化部署，一条命令即可启动：

```bash
docker-compose up --build
```

> 背后的架构是这样的：`docker-compose.yml` 定义了两个服务——Flask 应用容器和 Nginx 反向代理。Nginx 负责静态资源服务和 SSE 流式响应的透传配置（关键是 `X-Accel-Buffering: no` 头），Flask 负责所有业务逻辑。
>
> 启动完成后，我们在浏览器输入 `http://localhost`，可以看到赛博朋克风格的深色界面。顶部导航栏有四个页面：数据概览、智能问答、可视化仪表盘、分析报告。
>
> 这里有一个关键设计：服务重启后**无需重新上传数据**。[routes/api.py:82-84](routes/api.py#L82-L84) 中实现了持久化机制——每次上传文件路径会写入 `.last_upload.json`，AI 配置写入 `.ai_config.json`。服务重启时 `_try_auto_reload()` 自动从磁盘恢复状态，对话历史也通过 [ai/chat.py:154-161](ai/chat.py#L154-L161) 的 `_save_to_disk()` 持久化到 `.chat_history.json`。

### 画面：上传文件

> 现在上传一份数据集。支持 CSV、Excel、JSON 三种格式。上传后，后端会触发一条完整的处理流水线：

```
load_file() → Preprocessor.run_all() → Analyzer → Detector → InsightEngine → DataProfiler
```

> 这条流水线在 [routes/api.py:139-179](routes/api.py#L139-L179) 的 `_rebuild_state_from_file()` 中一次性完成，结果存入 Flask 的全局 `app.state` 字典，供所有后续 API 使用。

---

## 第二章：数据概览页 — 全数据集普适性（1:30 - 5:00）

### 画面：数据概览页

> 上传完成后自动跳转到数据概览页。这个页面包含**五大模块**，我逐一讲解。
>
> 但在讲解之前，我要先回答一个关键问题：**DataMind 如何做到对所有数据集通用？**

### 2.0 全数据集普适性架构（核心创新）

> 传统数据分析工具是"硬编码"的——针对特定数据集写死列名和分析逻辑。DataMind 的做法完全不同：**运行时自动检测数据集类型，动态适配分析策略**。
>
> 核心模块是 [data/profiler.py](data/profiler.py) 的 `DataProfiler`。它通过三层机制实现普适性：
>
> **第一层：列名语义推断**
>
> `DataProfiler` 内置了一组正则关键词库：
> ```python
> _DATE_KEYWORDS   = r"date|time|日期|时间|year|month|day|week"
> _CUSTOMER_KWORDS = r"customer|client|user|userid|客户|用户"
> _PRODUCT_KWORDS  = r"product|item|sku|description|商品|产品"
> _AMOUNT_KWORDS   = r"price|amount|revenue|sales|total|金额|价格|销售"
> _GEO_KEYWORDS    = r"country|province|city|region|state|国家|省|城市"
> ```
>
> 扫描所有列名，匹配到 `InvoiceDate` → 日期列，`CustomerID` → 客户列，`UnitPrice` → 金额列，`Country` → 地理列。**不需要用户手动指定任何列名**。
>
> **第二层：数据类型推断**
>
> 除了列名语义，还结合 pandas 的 dtype 检测：
> - `select_dtypes(include="number")` → 数值列
> - `select_dtypes(include=["datetime"])` → 日期列
> - `select_dtypes(include=["object", "category"])` + `nunique() <= 50` → 类别列
>
> **第三层：画像分类**
>
> 基于推断结果，`_classify_mode()` 将数据集分为 6 种画像：
>
> ```python
> if has_date and has_customer and (product_col or amount_col):
>     return "retail"       # 零售交易型
> if has_date and numeric_cols:
>     return "temporal"     # 时间序列型
> if has_geography and numeric_cols:
>     return "geographic"   # 地理分布型
> if len(numeric_cols) >= 4 and len(categorical_cols) <= 2:
>     return "numeric"      # 科学数值型
> if len(categorical_cols) >= 3:
>     return "categorical"  # 分类调查型
> return "mixed"            # 混合型（兜底）
> ```
>
> 这意味着：**同一份代码，上传电商数据自动走零售分析，上传鸢尾花数据自动走数值分析，上传问卷数据自动走分类分析**。不需要改一行代码。

### 2.1 指标卡片

> 顶部是四个指标卡片，展示总记录数、字段数、时间跨度、总销售额。
>
> 这些数据来自 [routes/api.py:279-285](routes/api.py#L279-L285) 的 `/api/data/summary` 接口，底层调用 `Analyzer.summary_stats()`。
>
> 关键细节：第 3、4 张卡片是**自适应的**。[templates/index.html:686-714](templates/index.html#L686-L714) 的 `_updateAdaptiveStatCards()` 会根据画像模式切换显示内容：
> - 零售/时序模式 → "时间跨度 + 总销售额"
> - 纯数值模式 → "数值列数 + 目标列均值"
> - 地理模式 → "数值列数 + 地理列名"
> - 分类模式 → "数值列数 + 类别列数"
>
> 前端有一个细节：数字使用 `animateNum()` 函数做**计数器动画**，800ms 内从 0 递增到目标值，视觉效果更流畅。

### 2.2 数据质量评分卡（零 API 依赖）

> 这是第一个创新点：**数据质量评分卡**。
>
> 它完全不依赖任何 AI API，而是通过 `QualityScorer` 模块的**纯规则引擎**实现。评分维度包括：
> - 完整性（缺失率）
> - 一致性（类型冲突）
> - 准确性（异常值比例）
> - 唯一性（重复率）
> - 时效性（数据新鲜度）
>
> 每个维度独立打分，加权汇总为 0-100 的总分和 A/B/C/D 等级。前端用 SVG 环形图 + 5 个进度条可视化展示。
>
> 这种设计的优势是：**零成本、毫秒级响应、结果完全可解释**。

### 2.3 数据自描述

> 第二个创新点：**数据自描述系统**。
>
> [data/profiler.py](data/profiler.py) 中的 `DataProfiler` 会自动检测数据集类型，支持 6 种画像模式：
> - `retail` — 零售电商（有 InvoiceNo、Quantity、UnitPrice 等特征列）
> - `temporal` — 时间序列（有日期列 + 数值列）
> - `numeric` — 纯数值（多列数值型）
> - `categorical` — 纯分类（多列类别型）
> - `geographic` — 地理数据（有国家/城市/经纬度列）
> - `mixed` — 混合型（兜底）
>
> 检测到画像后，概览页的第 3、4 张指标卡片会**自适应切换**。同时，`DataProfiler` 还会生成**建议问题列表**，直接注入到智能问答页的输入框中。
>
> 建议问题也是**按画像定制的**：
> - 零售模式 → "月均销售额是多少？"、"Top 10 畅销商品有哪些？"
> - 时序模式 → "按日期展示数值趋势折线图"
> - 分类模式 → "各类别的数量占比如何？"
> - 地理模式 → "各地区/国家的数据分布有什么差异？"

### 2.4 预处理 Pipeline 可视化

> 这是整个概览页最复杂的模块。我们设计了一个**7 步 Pipeline 流程图**：
>
> ```
> 01 去重 → 02 文本清洗 → 03 缺失值处理 → 04 类型转换
> → 05 无效过滤 → 06 异常标记 → 07 特征工程
> ```
>
> 每一步都展示具体统计：比如"移除 5,268 条重复"、"填充 2 列，其中 1 列高缺失率"、"标记 1,203 个异常值，涉及 3 列"。
>
> 这些数据来自 [routes/api.py:298-304](routes/api.py#L298-L304) 的 `/api/data/preprocess-report` 接口。前端 [templates/index.html:547-649](templates/index.html#L547-L649) 的 `renderPrepTimeline()` 函数负责渲染，每一步卡片有**错落出现动画**（100ms 间隔），最后显示 Pipeline 完成汇总条。
>
> 下方还有两个 Plotly 图表：清洗前后数据量对比柱状图 + 各列缺失率横向条形图，来自 `/api/analysis/preprocess_visual` 接口。

### 2.5 数据预览

> 最底部是数据预览表格，展示前 50 行原始数据，支持横向滚动。

---

## 第三章：智能问答页 — 技能路由与代码生成（5:00 - 10:00）

### 画面：智能问答页

> 这是 DataMind 最核心的页面，采用**双栏布局**：左侧是对话区，右侧是 NL2Vis 图表工作台。
>
> 我先演示一个完整的问答流程，再拆解背后的技术架构。

### 3.1 问答演示：技能路由命中

> 我在输入框输入："各国家的销售额占比是多少？"，点击发送。
>
> 大家注意看，左侧对话区出现了：
> 1. 一个**技能路由徽章**——"已选择技能：📊 分布分析"
> 2. 一个**证据表**——显示各国销售额数据
> 3. 流式输出的**中文解读**——AI 基于证据表生成的自然语言解释
> 4. 一个**交互式饼图**——各国销售额占比
>
> 同时，右侧图表工作台也**自动同步**了这张图表。

### 3.2 技能路由架构详解（核心创新）

> 传统做法是把用户问题直接丢给 LLM 生成代码。我们的做法是**两层路由**：
>
> **第一层：技能路由**（[ai/skill_router.py](ai/skill_router.py)）
>
> `SkillRouter.route()` 会做一次 LLM 调用。关键设计是：**所有 SKILL.md 文档会被注入到 system prompt 中**。[skills/__init__.py:51-55](skills/__init__.py#L51-L55) 的 `load_catalog()` 函数拼接所有技能文档：
>
> ```python
> def load_catalog() -> str:
>     docs = [f"## {spec.name}\n\n{spec.path.read_text(encoding='utf-8')}"
>             for spec in SKILLS.values()]
>     return "\n\n---\n\n".join(docs)
> ```
>
> 系统提示词中还注入了**字段 schema**（通过 [skills/_contract.py:29-40](skills/_contract.py#L29-L40) 的 `infer_schema()` 推断）和**数据样例**（前 5 行）。LLM 基于这些信息选择最匹配的技能。
>
> 我们有 6 个技能：
>
> | 技能 | 触发条件 | 输出 |
> |------|----------|------|
> | `stats-skill` | 用户问均值、中位数、方差等统计量 | 描述性统计 JSON |
> | `viz-skill` | 用户要画图 | 图表配置 JSON |
> | `trend-skill` | 用户问时间趋势、月度/周度变化 | 时间序列聚合 JSON |
> | `correlation-skill` | 用户问相关性、关联 | 相关性矩阵 JSON |
> | `distribution-skill` | 用户问分布、异常值、数据形状 | 直方图/箱线图 JSON |
> | `profile-skill` | 用户问数据概况、特征 | 数据画像 JSON |
> | `fallback` | 无技能命中 | 走代码生成兜底 |
>
> 每个 SKILL.md 都是一个**结构化的指令文档**，告诉 LLM：
> 1. 什么时候选择这个技能
> 2. 输出什么格式的 JSON
> 3. 有哪些约束规则
>
> 比如 [skills/trend_skill/SKILL.md](skills/trend_skill/SKILL.md)：
> ```markdown
> # Trend Skill
> You help a Python program plan a time-series trend, not write code.
> Return JSON only:
> {"date_col": "...", "value_col": "...", "agg": "sum|mean", "freq": "D|W|M|Q|Y"}
> ```
>
> LLM 返回的是**计划 JSON**，不是代码。这个计划会被传给对应的执行函数。
>
> **第二层：确定性执行 + LLM 解释**
>
> 命中技能后，系统执行的是**确定性 Python 脚本**，不是 LLM 生成的代码。
>
> 以趋势分析为例，[skills/trend_skill/trend_skill.py](skills/trend_skill/trend_skill.py) 的 `execute_trend_plan()` 做了这些事：
>
> 1. `_pick_date_col()` — 智能选择日期列（先检查 LLM 指定的列，再回退到第一个 datetime 列，再回退到能解析为日期的列）
> 2. `safe_col()` — 安全校验数值列（LLM 指定的列不存在时，降级到第一个数值列）
> 3. `pd.to_datetime()` + `pd.to_numeric()` + `dropna()` — 数据清洗
> 4. `resample(freq)` — 按频率重采样
> 5. `px.line()` — 生成 Plotly 折线图
> 6. 返回 `SkillResult(answer, evidence, chart)`
>
> 这里的**普适性关键**是 `safe_col()` 函数（[skills/_contract.py:43-56](skills/_contract.py#L43-L56)）：
> ```python
> def safe_col(df, value, prefer_numeric=False):
>     if value in df.columns:
>         if not prefer_numeric or pd.api.types.is_numeric_dtype(df[value]):
>             return str(value)
>     if prefer_numeric:
>         nums = df.select_dtypes(include="number").columns
>         if len(nums):
> return str(nums[0])
>     return str(df.columns[0])
> ```
>
> **不管 LLM 返回什么列名，只要数据集中有数值列，就能正确执行**。这是对所有数据集通用的关键。
>
> 执行完成后，产出一张**证据表**（pandas DataFrame）。
>
> 然后 [ai/skill_router.py:93-120](ai/skill_router.py#L93-L120) 的 `explain_stream()` 做第二次 LLM 调用，这次 AI 的角色是"数据解读员"——基于证据表流式生成中文解释。
>
> ```python
> system = "你是数据分析助教。下面是确定性脚本基于真实数据算出的证据表。"
>         "请用简洁中文解读结论，只依据证据表、不要编造数据、不要写代码。"
> ```
>
> 这个架构的核心优势是：**数据计算 100% 准确**（确定性脚本），**表达 100% 自然**（LLM 生成）。AI 永远不会编造数据，因为它只负责"解读"，不负责"计算"。

### 3.3 代码生成兜底

> 当技能路由返回 `fallback` 时，系统走**代码生成路径**。
>
> [ai/code_generator.py](ai/code_generator.py) 的 `CodeGenerator` 执行流程：
>
> ```
> 用户问题 → build_system_prompt() → OpenAI API → 提取 ```python``` 代码块
> → validate_code() 黑名单检查 → _sanitize_code() 移除 import
> → threading.Thread + exec() 沙箱执行（30s 超时）→ 结果序列化
> ```
>
> 这里有几个关键的安全设计：
>
> **双层安全防护**：第一层是 `_FORBIDDEN` 黑名单（`import os`、`subprocess`、`__import__`、`eval(` 等），第二层是受限命名空间——只暴露 `len`、`range`、`sorted` 等安全内置函数，数据集用 `df.copy()` 副本防止篡改。
>
> **threading 超时控制**：代码在子线程中执行，主线程通过 `done_event.wait(timeout=30)` 等待。超时后返回错误，不会卡死服务。Python 无法强制杀死线程，但 daemon 线程会在进程退出时自动回收。
>
> **Plotly Base64 解码**：plotly 5.x 将 numpy 数组序列化为 `{"dtype": "f8", "bdata": "<base64>"}` 格式，旧版 Plotly.js 无法解码会导致散点图空白。[ai/code_generator.py:330-355](ai/code_generator.py#L330-L355) 的 `_decode_plotly_bdata()` 递归解码所有 binary 字段，确保跨版本兼容。

### 3.4 NL2Vis 图表工作台

> 右侧是图表工作台，支持**自然语言生成图表**。
>
> 我输入："画一个各月销售额的折线图"，它会调用 `/api/chart/generate` 接口，底层是 [ai/chart_generator.py](ai/chart_generator.py) 的 `ChartGenerator`。
>
> 关键创新是**迭代修改支持**：生成图表后，我可以继续说"改成莫兰迪色系"或"只看 Top5"，AI 会基于上一张图表的结构摘要进行增量修改。
>
> [ai/chart_generator.py:154-247](ai/chart_generator.py#L154-L247) 的 `_summarize_previous_chart()` 不是粗暴截断原始 JSON（可能超过 100KB），而是智能提取：
> - Trace 类型、数量、名称
> - 颜色配置（marker.color、line.color）
> - 数据形状（前 3 个值 + 总数）
> - Layout 配置（barmode、legend、annotations）
>
> 前端 [static/js/chart-workspace.js:142-190](static/js/chart-workspace.js#L142-L190) 的 `_preparePreviousChart()` 也会做预处理：剥离 layout.template（可达 50KB+），对长数组保留前 5 + 后 2 个采样值。

### 3.5 智能问答 ↔ NL2Vis 双向同步（代码级原理）

> 这是整个系统最精妙的设计：**对话区和图表工作台的双向同步**。
>
> 为什么两个独立组件能始终保持同步？核心在于三个设计：**共享全局状态**、**单向数据流**、**函数注入**。

#### 3.5.1 全局状态层：唯一的"真相源"

> [static/js/chart-workspace.js:11-13](static/js/chart-workspace.js#L11-L13) 定义了三个全局变量：
>
> ```javascript
> window._chartWorkspace = null;
> window._currentChartData = null;   // ← 当前图表的 Plotly JSON（唯一真相源）
> window._currentChartCode = '';     // ← 生成图表的 Python 代码
> ```
>
> 这三个变量挂在 `window` 上，**两个组件（chat.js 和 chart-workspace.js）都能读写同一个引用**。这是同步的物理基础——没有消息总线、没有事件订阅、没有 WebSocket，就是最朴素的共享内存。

#### 3.5.2 方向一：问答 → 工作台（SSE 流驱动）

> 用户在对话区提问后，后端通过 SSE 流推送 `chart` 事件。前端 [static/js/chat.js:91-103](static/js/chat.js#L91-L103) 的 `onChart` 回调处理：
>
> ```javascript
> onChart: function (chartData) {
>     // 步骤 1：在聊天气泡中内联渲染图表（对话区自己的渲染）
>     _injectChart(bubble, chartData);
>
>     // 步骤 2：调用工作台的 renderChart()，更新右侧图表
>     if (typeof renderChart === 'function') {
>         renderChart(chartData);
>     }
>
>     // 步骤 3：更新全局状态（关键！）
>     window._currentChartData = chartData;
>
>     // 步骤 4：更新状态提示
>     statusEl.textContent = '✓ 图表已同步自对话';
> }
> ```
>
> 为什么能调用 `renderChart()`？因为 [static/js/chart-workspace.js:197](static/js/chart-workspace.js#L197) 中这个函数定义在**全局作用域**（没有 `var`/`let`/`const` 前缀），chat.js 可以直接调用。
>
> 执行顺序：**对话区渲染 → 工作台渲染 → 更新全局状态**。三步在同一个同步调用栈中完成，没有异步延迟。

#### 3.5.3 方向二：工作台 → 问答（函数注入驱动）

> 用户在工作台输入"改成莫兰迪色系"后，[static/js/chart-workspace.js:69-106](static/js/chart-workspace.js#L69-L106) 的 `generateChart()` 发起 fetch 请求，成功后：
>
> ```javascript
> .then(function (data) {
>     if (data.success && data.chart) {
>         // 步骤 1：更新全局状态
>         _currentChartData = data.chart;
>         _currentChartCode = data.code || '';
>
>         // 步骤 2：渲染工作台图表
>         renderChart(data.chart);
>
>         // 步骤 3：调用对话区的同步函数
>         if (typeof window.updateChatChartFromWorkspace === 'function') {
>             window.updateChatChartFromWorkspace(data.chart);
>         }
>     }
> })
> ```
>
> 关键问题：`updateChatChartFromWorkspace` 是在 chat.js 中定义的，chart-workspace.js 怎么能调用它？
>
> 答案是**函数注入**：[static/js/chat.js:423](static/js/chat.js#L423) 将函数挂到 `window` 上：
>
> ```javascript
> window.updateChatChartFromWorkspace = function (chartData) {
>     if (!chartData || !window.Plotly) return;
>     var container = document.getElementById('chat-messages');
>
>     // 查找对话区已有的图表 DOM
>     var inlineCharts = container.querySelectorAll('.exec-chart-inline');
>
>     if (inlineCharts.length > 0) {
>         // 已有图表 → 就地更新最后一个（Plotly.react 高效 diff 更新）
>         var lastChart = inlineCharts[inlineCharts.length - 1];
>         var traces = (chartData.data !== undefined) ? (chartData.data || []) : [];
>         var layout = _cleanInjectLayout(chartData.layout || {});
>         traces = _ensureChatMarkerVisibility(traces);
>         Plotly.react(lastChart, traces, layout, { responsive: true });
>     } else {
>         // 无已有图表 → 新建一条系统消息展示工作台图表
>         _appendWorkspaceChartMessage(chartData);
>     }
> };
> ```
>
> 这里的 `Plotly.react()` 是 Plotly 的高效更新 API——它会 diff 新旧数据，只更新变化的部分，不会重建整个图表 DOM。

#### 3.5.4 迭代修改时的状态流转

> 当用户在工作台连续修改图表时，`_currentChartData` 始终保存**最新的图表状态**。[static/js/chart-workspace.js:67](static/js/chart-workspace.js#L67) 中：
>
> ```javascript
> var cleanedChart = _preparePreviousChart(_currentChartData);
> ```
>
> `_preparePreviousChart()` 会把当前图表作为 `previous_chart` 发送给后端，AI 基于它做增量修改。修改完成后，新图表又写回 `_currentChartData`。
>
> 完整的状态流转：
>
> ```
> 用户在工作台输入 "改成柱状图"
>       │
>       ▼
> _preparePreviousChart(_currentChartData)  ← 读取当前状态
>       │
>       ▼
> POST /api/chart/generate { description, previous_chart }
>       │
>       ▼
> ChartGenerator.generate() → AI 基于 previous_chart 生成新代码
>       │
>       ▼
> 返回新 chart JSON
>       │
>       ▼
> _currentChartData = data.chart    ← 更新全局状态
> renderChart(data.chart)           ← 更新工作台
> updateChatChartFromWorkspace()    ← 更新对话区
>       │
>       ▼
> 下一次修改时，_currentChartData 已是最新版本
> ```

#### 3.5.5 为什么不会出现不同步？

> 有三个保障机制：
>
> **保障 1：单写多读**
>
> `_currentChartData` 只在两个地方被写入：
> - chat.js 的 `onChart` 回调（问答产出图表时）
> - chart-workspace.js 的 `generateChart().then()`（工作台产出图表时）
>
> 读取方只有 `_preparePreviousChart()`（发送给后端时）。写入点少，冲突概率低。
>
> **保障 2：同步调用，无竞态**
>
> 两个方向的更新都在同一个同步调用栈中完成（不是 setTimeout 或 Promise 回调），浏览器主线程是单线程的，不存在竞态条件。
>
> **保障 3：防御性渲染**
>
> `updateChatChartFromWorkspace()` 做了防御性检查：
> - `if (!chartData || !window.Plotly) return` — 空数据不渲染
> - `if (inlineCharts.length > 0)` — 有旧图表就更新，没有就新建
> - `_cleanInjectLayout()` — 剥离 template 防止样式冲突
> - `_ensureChatMarkerVisibility()` — 强制 scatter marker 可见
>
> 这种设计的优势是：**零耦合（两个 JS 文件互不 import）、零延迟（同步调用栈）、零依赖（不需要 WebSocket、EventEmitter 或状态管理库）**。

### 3.6 不同技能的调用演示

> 现在我演示不同类型的用户问题，展示技能路由如何自动选择不同的技能：
>
> **演示 1：统计分析**
>
> 输入："UnitPrice 的均值和中位数是多少？"
>
> → 路由命中 `stats-skill` → 执行 `execute_stats_plan()` → 返回描述性统计证据表
>
> **演示 2：趋势分析**
>
> 输入："月度销售额的变化趋势是什么？"
>
> → 路由命中 `trend-skill` → 执行 `execute_trend_plan()` → 按月重采样 → 返回折线图
>
> **演示 3：相关性分析**
>
> 输入："Quantity 和 UnitPrice 之间有相关性吗？"
>
> → 路由命中 `correlation-skill` → 执行 `execute_correlation_plan()` → 返回相关系数 + 散点图
>
> **演示 4：分布分析**
>
> 输入："UnitPrice 的分布情况如何？"
>
> → 路由命中 `distribution-skill` → 执行 `execute_distribution_plan()` → 返回直方图 + 统计摘要
>
> **演示 5：兜底代码生成**
>
> 输入："计算每个客户的 RFM 评分"
>
> → 路由返回 `fallback`（无技能匹配）→ 走代码生成路径 → AI 生成 Python 代码 → 沙箱执行
>
> 大家可以看到，**同一套系统，根据用户问题的语义自动选择最优的执行路径**。简单问题走技能（快速、准确、低成本），复杂问题走代码生成（灵活、通用）。

---

## 第四章：可视化仪表盘 — 自适应图表（10:00 - 11:30）

### 画面：可视化仪表盘

> 仪表盘页展示 6 个 Plotly 交互图表，完全**自适应数据画像**。
>
> [routes/api.py:356-477](routes/api.py#L356-L477) 的 `/api/analysis/adaptive_charts` 接口根据数据画像模式选择图表组合：
>
> | 模式 | 图表 1 | 图表 2 | 图表 3 | 图表 4 | 图表 5 | 图表 6 |
> |------|--------|--------|--------|--------|--------|--------|
> | retail | 销售趋势 | 热销商品 | 地区分布 | 相关性矩阵 | 时段热力图 | RFM 分析 |
> | temporal | 时间折线 | 相关性矩阵 | 箱线图 | 散点图 | 目标列分布 | 清洗对比 |
> | numeric | 直方图 | 相关性矩阵 | 箱线图 | 散点图 | 数值分布 | 清洗对比 |
> | categorical | 类别频次 | 类别频次 | 类别频次 | 类别频次 | 类别频次 | 清洗对比 |
>
> 纯分类数据（无数值列）时，全部 6 个图表都使用类别频次图，**不会出现空白卡片**。这是通过 `has_numeric` 标志位自适应切换的。
>
> 每个图表都是 Plotly 交互式的：支持缩放、悬停查看数据、框选、下载 PNG。

---

## 第五章：分析报告 — 多 Agent 协作（11:30 - 14:00）

### 画面：分析报告页

> 报告页支持三种模式：简洁报告、深度报告、数据故事。

### 5.1 简洁报告

> 简洁模式是单次 AI 调用，将数据摘要 + 洞察列表 + 对话历史发给 LLM，生成约 500 字的 Markdown 报告。
>
> AI 失败时自动降级为**纯模板报告**（[ai/report.py:257-298](ai/report.py#L257-L298) 的 `_fallback_report()`），保证报告始终可输出。

### 5.2 深度报告（多 Agent 协作框架）

> 这是重点创新：**四 Agent 协作生成约 3000 字深度报告**。
>
> 架构设计在 [ai/report_agents.py](ai/report_agents.py) 中，四个 Agent 各司其职：
>
> ```
> ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
> │ StatisticsAgent │  │   InsightAgent  │  │     QAAgent     │
> │ 数据特征统计    │  │ 关键洞察解读    │  │ 对话问答摘要    │
> └────────┬────────┘  └────────┬────────┘  └────────┬────────┘
>          │                    │                    │
>          └────────────────────┼────────────────────┘
>                               │
>                     ┌─────────▼─────────┐
>                     │  SynthesisAgent   │
>                     │ 综合总结与建议    │
>                     └───────────────────┘
> ```
>
> **Agent 1：StatisticsAgent** — 输入是 `df_info`（数据集摘要 + 数值统计），输出是"数据特征描述"章节。
>
> **Agent 2：InsightAgent** — 输入是 `insights`（InsightEngine 自动生成的洞察列表），输出是"关键洞察深度解读"章节。按 HIGH/MEDIUM/LOW 分组展示。
>
> **Agent 3：QAAgent** — 输入是 `chat_history`（用户与 AI 的对话历史），输出是"对话分析摘要"章节。用叙事性文字呈现分析探索过程。
>
> **Agent 4：SynthesisAgent** — 输入是前三个 Agent 的输出，输出是"总结与建议"章节。生成执行摘要和 3-5 条可执行建议。
>
> 关键设计：**每个 Agent 都有独立的 `_fallback()` 方法**。任意 Agent 失败不影响其他 Agent，保证报告始终可输出。
>
> 在 SSE 流模式下，每个 Agent 的产出会实时推送到前端，用户可以看到报告**逐章节生成**的过程。

### 5.3 数据故事引擎（华尔街日报体）

> 第三种模式是**数据叙事故事**，这是最具创意的功能。
>
> [ai/storyteller.py](ai/storyteller.py) 的 `Storyteller` 使用"华尔街日报体"叙事弧线：
>
> ```
> 开篇钩子 → 背景铺垫 → 数据深潜 → 结论启示
> ```
>
> System prompt 要求 AI 生成严格 JSON 格式：
> ```json
> {
>   "title": "Q4 暴增 45%：一场促销如何改写全年业绩",
>   "subtitle": "第四季度销售额创历史新高",
>   "sections": [
>     {"heading": "...", "body": "...", "highlight": "Q4 增长 45%"}
>   ],
>   "key_takeaways": ["结论1", "结论2", "结论3"]
> }
> ```
>
> 标题必须是**数据驱动的判断句**，而非"XX 分析报告"这种模板化标题。每个数字必须有上下文对比（同比/环比/占比），善用"相当于""超过 x 倍"让数字可感知。
>
> JSON 解析有**三级容错**：先尝试裸 JSON 解析，再尝试提取 ` ```json``` ` 代码块，最后用正则 `\{.*\}` 提取。解决了 AI 输出 JSON 格式不稳定的问题。
>
> AI 不可用时，`_fallback()` 方法基于数据特征和洞察列表生成基础叙事，保证功能始终可用。

---

## 第六章：零 API 依赖的洞察引擎（14:00 - 15:30）

### 画面：回到数据概览页的洞察卡片

> 最后讲解一个贯穿整个系统的底层模块：**规则引擎自动洞察**。
>
> [ai/insight.py](ai/insight.py) 的 `InsightEngine` 完全不依赖任何 LLM API，通过纯规则 + 统计阈值生成洞察：
>
> | 洞察类型 | 规则 | 阈值 |
> |----------|------|------|
> | 趋势 | MoM 增长/下跌 | ≥20% medium, ≥50% high |
> | 异常 | IQR 异常率 | ≥5% medium, ≥10% high |
> | 分布 | 第一名占比 | ≥60% medium, ≥80% high |
> | 相关性 | \|Pearson r\| | ≥0.70 medium, ≥0.85 high |
> | 周期 | 购买高峰 | 自动检测 |
> | 偏态 | 偏态系数 | >1.5 |
> | 类别集中 | 最高频类别占比 | >60% |
> | 目标相关 | 与目标列相关系数 | >0.3 |
>
> 输出格式统一为：`{type, severity, title, detail}`。
>
> 这个引擎的优势是：**零成本（无 token 消耗）、毫秒级响应、结果完全可解释（每条洞察都有明确的阈值和计算逻辑）**。
>
> 它是整个系统的"地基"——数据概览页的洞察卡片、深度报告的 InsightAgent 输入、数据故事的素材，都来自这个引擎。

---

## 第七章：总结与技术亮点回顾（15:30 - 16:30）

> 总结一下 DataMind 的核心技术亮点：
>
> 1. **全数据集普适性**：DataProfiler 运行时检测 6 种数据画像，自动适配分析策略、图表组合和建议问题，同一套代码处理任意数据集
>
> 2. **技能路由 + 确定性执行**：6 个专用技能 + 1 个兜底代码生成，数据计算用确定性脚本，AI 只负责解读，杜绝编造数据
>
> 3. **智能问答 ↔ NL2Vis 双向同步**：全局变量桥梁，零耦合零延迟
>
> 4. **四 Agent 协作报告**：每个 Agent 独立降级，保证报告始终可输出
>
> 5. **华尔街日报体数据叙事**：从"数据罗列"进化到"讲故事"
>
> 6. **零 API 依赖的规则引擎**：洞察、质量评分、数据画像全部本地计算
>
> 7. **双层安全沙箱**：黑名单 + 受限命名空间 + threading 超时
>
> 8. **全链路降级保障**：每个 AI 调用都有 fallback，服务永不失效
>
> 9. **Plotly Base64 解码**：解决 plotly 5.x 散点图空白的跨版本兼容问题
>
> 10. **容器化一键部署**：Docker + Nginx + 持久化状态恢复
>
> 感谢观看！

---

## 附录：演示操作清单

| 时间点 | 操作 | 重点讲解 |
|--------|------|----------|
| 0:00 | 终端启动 Docker | 容器化架构 + Nginx SSE 配置 |
| 0:30 | 浏览器输入 localhost | 赛博朋克 UI + 持久化状态恢复 |
| 0:45 | 上传 CSV 文件 | 处理流水线 + DataProfiler 画像检测 |
| 1:30 | 查看指标卡片 | 自适应卡片（按画像切换第 3/4 卡） |
| 2:00 | 查看质量评分卡 | 零 API 依赖规则引擎 |
| 2:30 | 查看数据自描述 | 6 种画像 + 建议问题自动生成 |
| 3:00 | 查看 Pipeline 可视化 | 7 步流程 + 错落动画 |
| 5:00 | 问答："各国家销售额占比" | 技能路由 → distribution-skill → 证据表 + 流式解释 |
| 6:00 | 查看同步到工作台的图表 | 双向同步机制 |
| 6:30 | 工作台："改成莫兰迪色系" | 迭代修改 + 智能摘要 |
| 7:00 | 问答："UnitPrice 均值中位数" | 技能路由 → stats-skill |
| 7:30 | 问答："月度销售趋势" | 技能路由 → trend-skill |
| 8:00 | 问答："Quantity 和 UnitPrice 相关性" | 技能路由 → correlation-skill |
| 8:30 | 问答："UnitPrice 分布" | 技能路由 → distribution-skill |
| 9:00 | 问答："计算 RFM 评分" | 无技能命中 → fallback → 代码生成兜底 |
| 10:00 | 切换到可视化仪表盘 | 自适应图表（6 种画像 × 6 图表） |
| 11:30 | 生成简洁报告 | 单次 AI + 模板降级 |
| 12:00 | 生成深度报告 | 四 Agent 协作（Statistics → Insight → QA → Synthesis） |
| 13:00 | 查看数据故事 | 华尔街日报体叙事 + JSON 三级容错 |
| 14:00 | 回到概览页查看洞察 | 规则引擎 8 种洞察类型 + 阈值表 |
| 15:30 | 总结 | 十大技术亮点 |
