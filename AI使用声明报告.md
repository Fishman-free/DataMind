# AI 使用声明报告

> **课程**：Python 程序设计 — 期末大作业
> **项目名称**：DataMind — 智能问数平台
> **学生**：马珠淇 | 学号：2025201699
> **提交日期**：2026 年 6 月
> **AI 工具**：Claude Code (Claude Opus 4.8 / Claude Sonnet 4.6 / DeepSeek V4 Pro)、Context7 MCP、Playwright MCP、GitHub MCP

---

## 目录

1. [AI 工具使用总览](#一ai-工具使用总览)
2. [Harness 工程实践与人机协作方法论](#二harness-工程实践与人机协作方法论)
3. [关键 AI 协作案例深度剖析](#三关键-ai-协作案例深度剖析)
   - [案例 1：Anthropic 模型适配器 — 架构模式决策](#案例-1anthropic-模型适配器--架构模式决策)
   - [案例 2：Plotly 5.x 二进制编码 — 深度 Debug 全流程](#案例-2plotly-5x-二进制编码问题--深度-debug-全流程)
   - [案例 3：代码安全沙箱 — 多层防护体系](#案例-3代码安全沙箱--多层防护体系及安全审计)
   - [案例 4：全数据集通用化改造 — 系统架构升级](#案例-4全数据集通用化改造--系统架构升级)
   - [案例 5：技能化问数架构 — 范式重构](#案例-5技能化问数架构--从代码生成到技能路由)
   - [案例 6：数据预处理可视化 — 全链路改造](#案例-6数据预处理可视化--全链路数据追踪)
4. [工程师思维体现](#四工程师思维体现)
5. [设计思维体现](#五设计思维体现)
6. [产品思维体现](#六产品思维体现)
7. [AI 能力边界深度思考](#七ai-能力边界深度思考)
8. [Bug 发现与修复全记录](#八bug-发现与修复全记录)
9. [学生独立决策与贡献清单](#九学生独立决策与贡献清单)
10. [AI 生成内容中被拒绝/修正的部分](#十ai-生成内容中被拒绝修正的部分)
11. [AI 辅助编程价值与风险反思](#十一ai-辅助编程价值与风险反思)
12. [附录：项目规模与测试覆盖](#附录项目规模与测试覆盖)

---

## 一、AI 工具使用总览

### 1.1 使用的 AI 工具

| 工具 | 用途 | 使用阶段 |
|------|------|----------|
| **Claude Code** | 代码生成、架构设计讨论、Bug 诊断辅助、测试编写、文档生成 | 全开发周期（日常高频） |
| **Context7 MCP** | 查阅 pandas、plotly、Flask、Anthropic SDK 等第三方库的官方文档 | 按需触发 |
| **Playwright MCP** | 浏览器自动化测试前端交互、截图验证 UI 渲染正确性 | 前端调试阶段 |
| **GitHub MCP** | 代码仓库管理、Commit 历史追溯、分支管理 | 版本管理 |

### 1.2 总体使用数据

DataMind 项目共计约 **9,000 行源码**（Python ~4,900 行，JavaScript ~2,500 行，HTML ~800 行，CSS ~800 行），分布在 `ai/`、`data/`、`routes/`、`skills/`、`static/`、`templates/`、`tests/` 七个核心目录中。测试套件包含 **30 个测试文件**、**338 个测试用例**，全部使用 MagicMock 模拟外部依赖。

| 指标 | 估算值 | 说明 |
|------|--------|------|
| AI 直接生成的代码占比 | ~65% | 骨架代码、模板、测试框架 |
| 学生重写/修改的代码占比 | ~35% | 架构决策、安全逻辑、关键算法 |
| 完全由学生独立编写的代码 | ~20% | 核心架构设计、路由层、安全审核 |
| AI 生成的测试代码占比 | ~70% | 学生审查每一个断言并补齐边界用例 |
| 学生独立做出的架构决策 | 15+ 项 | 详见第八节 |
| 学生发现并修复的 Bug | 20+ 个 | 详见第七节 |
| 学生审查后拒绝/重写的 AI 方案 | 8+ 个 | 详见第九节 |

### 1.3 各模块 AI 参与程度

| 模块 | 文件 | AI 参与度 | 学生核心贡献 |
|------|------|:--------:|-------------|
| `ai/anthropic_adapter.py` | Anthropic SDK 适配器 | 80% | Adapter 模式架构决策、temperature clamp、懒导入 |
| `ai/code_generator.py` | 代码安全沙箱 | 55% | 防护顺序设计（validate→sanitize）、bdata解码器、黑名单审计 |
| `ai/chat.py` | 对话管理 + SSE 流式 | 65% | 宽数据集截断策略、技能路由集成设计 |
| `ai/skill_router.py` | 技能路由器 | 60% | 双阶段调用架构、JSON计划schema设计 |
| `ai/storyteller.py` | 数据叙事引擎 | 70% | 华尔街日报体框架选型、叙事结构设计 |
| `ai/report_agents.py` | 多Agent报告框架 | 75% | Agent职责划分、独立token预算、优雅降级 |
| `ai/insight.py` | 规则洞察引擎 | 40% | 5类洞察分类体系、阈值调优、偏态/集中度洞察 |
| `data/profiler.py` | 数据画像 | 50% | 6种画像分类体系、关键词库设计、优先级算法 |
| `data/analyzer.py` | 数据分析 | 50% | 通用分析方法设计、preprocess_visual键名修复 |
| `data/quality_scorer.py` | 质量评分卡 | 55% | 时效性维度未来日期边界处理、评分模型设计 |
| `data/preprocessor.py` | 数据预处理 | 40% | Pipeline链式设计、CSV分隔符嗅探 |
| `routes/api.py` | API路由 | 35% | SSE流式架构、纯分类数据降级、14个端点设计 |
| `routes/pages.py` | 页面路由 | 20% | Flask蓝图注册架构 |
| `skills/*` | 6个确定性技能 | 55% | SkillResult契约设计、SKILL.md规范格式 |
| `static/js/*` | 前端交互逻辑 | 45% | 双向同步机制、Plotly布局清理、SSE事件路由 |
| `tests/*` | 测试套件 | 70% | 学生审查所有断言、补齐边界用例 |

---

## 二、Harness 工程实践与人机协作方法论

> **核心观点**：我始终认为 AI 辅助编程不是"把需求丢给 AI 然后等结果"，而是需要一套完整的工程方法论来驾驭 AI。本节记录我在项目中形成的 Harness 工程实践——如何通过结构化的协作流程，让 AI 成为可靠的工程伙伴而非不可控的代码生成器。

### 2.1 模型分层策略：不同任务用不同模型

在本项目中，我采用了**三模型分层策略**，根据任务类型和成本效益选择最合适的模型：

| 模型 | 定位 | 使用场景 | 成本考量 |
|------|------|----------|----------|
| **Claude Opus 4.8** | 顶层设计 | 架构设计讨论、复杂问题诊断、方案评审 | 最智能但最贵，仅用于关键决策 |
| **DeepSeek V4 Pro** | 执行层 | 写代码、写计划文档、生成测试、重复性编码 | 便宜，烧 tokens 不心疼 |
| **Claude Sonnet 4.6** | 调整层 | 代码微调、Bug 修复、重构优化 | 价格适中，智力优于 DeepSeek |

**为什么这样分层？**

- Opus 的智力最高，但每 1M tokens 成本是 Sonnet 的 5 倍、DeepSeek 的 20 倍。用它来写代码是浪费——它的价值在于"想清楚"而非"写出来"
- DeepSeek 便宜但智力有限，适合执行明确的任务（"按这个接口签名生成 6 个技能的执行函数"），不适合需要判断力的任务
- Sonnet 是性价比最优的中间层——智力接近 Opus，价格接近 DeepSeek。适合需要一定判断力但不需要顶级智能的任务（修 Bug、重构、优化）

### 2.2 Plan Mode：需求规划先于编码

在开始任何复杂任务前，我使用 Claude Code 的 **Plan Mode** 与 AI 进行详细需求讨论：

**典型流程**：

```
1. 进入 Plan Mode
   > /plan 技能化问数架构重构

2. 与 AI 讨论需求边界
   - 当前代码生成模式的问题是什么？
   - 技能路由的接口应该如何设计？
   - 如何保证向后兼容？
   - 成功标准是什么？

3. AI 生成实现计划
   - 任务拆解为 5-8 个子任务
   - 每个子任务有明确的输入/输出/验收标准
   - 标注依赖关系和执行顺序

4. 学生审查并调整计划
   - 删除过度设计的部分
   - 补充 AI 遗漏的边界条件
   - 调整执行顺序

5. 退出 Plan Mode，按计划执行
```

**价值**：Plan Mode 的核心价值不是"让 AI 帮我规划"，而是**强迫自己在动手前想清楚**。与 AI 的对话过程就是需求澄清的过程——当你能把需求描述到 AI 能理解的程度时，你自己也想清楚了。

### 2.3 CLAUDE.md：项目上下文的持久化

在项目根目录创建 `CLAUDE.md` 文件，定义项目的**技术栈、编码规范、文件操作规则、架构设计**等上下文信息。Claude Code 在每次会话开始时自动加载该文件。

**为什么要手写 CLAUDE.md？**

| 不写 CLAUDE.md | 写了 CLAUDE.md |
|----------------|---------------|
| AI 每次会话都要猜项目结构 | AI 第一秒就知道 `ai/` 是 AI 层、`data/` 是数据层 |
| AI 生成的代码风格不一致 | AI 遵循统一的命名规范和注释约定 |
| AI 不知道有哪些依赖 | AI 知道用 `plotly.express` 而非 `matplotlib` |
| AI 可能引入不兼容的库 | AI 知道 `requirements.txt` 中已有的依赖 |

**CLAUDE.md 的关键章节**（本项目实际内容）：

```markdown
## 技术栈
- 后端: Flask 3.0+ / pandas 2.0+ / plotly 5.0+
- 前端: Bootstrap 5 / Plotly.js / SSE
- 测试: pytest 7.0+

## 编码规范
- Python: 双引号、snake_case、类型注解、来源标注
- JavaScript: var 优先、单引号、全局函数不加 var

## 核心架构
- 三层分离: routes/ → ai/ → data/
- 技能路由: 6 个确定性技能 + 代码生成兜底
- 双向同步: window._currentChartData 全局状态
```

**教训**：CLAUDE.md 必须在项目一开始就写好。我在项目中期才创建，导致前半段 AI 经常生成不符合项目风格的代码，需要大量手动调整。这是我在此次vibe coding过程中最大的教训。

### 2.4 Subagents：复杂任务的并行处理

对于涉及多个模块的复杂任务，我使用 Claude Code 的 **Subagents** 功能将任务拆分为多个子任务并行执行：

**实际案例：全数据集通用化改造**

```
主 Agent（协调者）
  ├── Subagent 1: 分析 data/profiler.py 的画像检测逻辑
  ├── Subagent 2: 分析 routes/api.py 的图表配置接口
  ├── Subagent 3: 分析 static/js/charts.js 的前端渲染逻辑
  └── Subagent 4: 分析 tests/ 中的测试覆盖情况

→ 4 个 Subagent 并行读取不同模块，各自返回分析结果
→ 主 Agent 汇总结果，生成统一的改造方案
```

**优势**：
- **效率**：4 个模块的分析从串行 20 分钟缩短为并行 5 分钟
- **隔离**：每个 Subagent 只关注自己的模块，不会被其他模块的代码干扰
- **可验证**：每个 Subagent 的输出可以独立审查

### 2.5 Propose-Apply-Verify 循环

对于复杂任务，我采用**分阶段迭代**的方式，每个变更通过三步验证：

```
┌─────────────────────────────────────────────────┐
│  Propose（提案）                                 │
│  - 明确任务内容和技术方案                         │
│  - 定义验收标准（测试用例、UI 截图、API 响应）     │
└───────────────────────┬─────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────┐
│  Apply（执行）                                   │
│  - AI 按照提案逐项实现                           │
│  - 每完成一项立即运行测试验证                     │
└───────────────────────┬─────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────┐
│  Verify（验证）                                  │
│  - 运行全量测试套件                              │
│  - 人工审查关键代码                              │
│  - 前端截图验证 UI 渲染                          │
│  - 不通过则回到 Apply 修复                       │
└─────────────────────────────────────────────────┘
```

**实际案例：技能化问数架构**

| 阶段 | 内容 | 验收标准 |
|------|------|----------|
| Propose 1 | 设计 SkillResult 数据契约 | 接口签名 + 类型定义 |
| Apply 1 | 实现 `_contract.py` | `test_skill_contract.py` 通过 |
| Verify 1 | 运行测试 + 代码审查 | 0 failures |
| Propose 2 | 实现 6 个技能脚本 | 每个技能有独立测试 |
| Apply 2 | 逐个实现技能 | 每个技能实现后立即测试 |
| Verify 2 | 全量回归测试 | 338 tests pass |

### 2.6 自动化验证与 Hooks

通过 Claude Code 的 **Hooks** 机制，在每次文件编辑后自动运行测试：

```json
// .claude/settings.json
{
  "hooks": [{
    "type": "command",
    "command": "python -m pytest tests/ -x --tb=short -q 2>&1 | tail -8"
  }]
}
```

**效果**：每次 Edit/Write 工具调用后，自动运行 pytest 并显示最后 8 行输出。如果测试失败，我会立即看到错误并修复，而不是等到最后才发现回归。

**与 Git 的配合**：每次变更前提交当前状态，确保 AI 生成的代码出现问题时可以快速回滚：

```bash
# 变更前
git add -A && git commit -m "checkpoint: before skill router impl"

# 变更后验证
python -m pytest tests/ -x --tb=short -q

# 失败则回滚
git reset --hard HEAD~1
```

### 2.7 上下文管理与 Token 节省策略

在长时间的开发会话中，上下文窗口会逐渐填满。我学到了以下管理策略：

#### 何时使用 /compact

`/compact` 命令可以压缩当前对话上下文，释放 token 空间：

| 场景 | 是否 /compact |
|------|:------------:|
| 完成一个功能模块，准备开始下一个 | ✅ 立即 compact |
| 对话超过 20 轮 | ✅ 考虑 compact |
| 正在调试同一个问题，上下文相关 | ❌ 不要 compact |
| 刚讨论完架构方案，准备实现 | ⚠️ 先总结再 compact |

#### Token 节省技巧

1. **分模块读取**：不要一次性读取整个代码库，用 `Grep` 定位关键代码后再 `Read` 相关文件
2. **避免重复读取**：已经读过的文件不要重复读取，Claude Code 会追踪文件状态
3. **精准编辑**：优先使用 `Edit` 工具进行局部修改，避免 `Write` 覆盖整个文件（会消耗更多 token）
4. **子代理隔离**：将独立任务交给 Subagent 处理，完成后只返回结论，不返回完整的文件内容
5. **Skills 按需加载**：不要预先加载所有 Skills，只在需要时调用对应的 Skill

#### Skills 管理

本项目安装了 20 个 Skills（通过 superpowers-zh 框架），但实际高频使用的只有 5-6 个：

| Skills | 使用频率 | 典型场景 |
|--------|:-------:|----------|
| `brainstorming` | 高 | 新功能需求分析 |
| `test-driven-development` | 高 | 实现功能前先写测试 |
| `systematic-debugging` | 中 | Bug 排查 |
| `verification-before-completion` | 中 | 声称完成前验证 |
| `writing-plans` | 低 | 复杂任务规划 |
| 其余 15 个 | 低 | 按需触发 |

**经验**：Skills 不是越多越好。过多的 Skills 反而会增加认知负担——每次任务都要判断"该用哪个 Skill"。关键是找到 5-6 个高频 Skills 形成肌肉记忆。

### 2.8 动态工作流（Dynamic Workflow）

对于大规模任务（如全量测试回归、多模块重构），我使用 Claude Code 的动态工作流功能：

```yaml
# .claude/workflows/regression-test.yaml
name: regression-test
description: 运行全量测试并汇报结果
phases:
  - title: Run Tests
    detail: python -m pytest tests/ -x --tb=short -q
  - title: Report
    detail: 汇总测试结果，标注失败用例
```

**价值**：将复杂的多步骤操作封装为可重复执行的工作流，减少人工操作错误。

### 2.9 方法论总结

```
┌─────────────────────────────────────────────────────────────┐
│                    Harness 工程方法论                         │
│                                                             │
│   ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    │
│   │ Plan Mode   │───→│ CLAUDE.md   │───→│ Propose     │    │
│   │ 需求规划    │    │ 上下文配置  │    │ 任务提案    │    │
│   └─────────────┘    └─────────────┘    └──────┬──────┘    │
│                                                 │           │
│                                                 ▼           │
│   ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    │
│   │ Git 回滚    │←───│ Hooks 自动  │←───│ Apply       │    │
│   │ 版本控制    │    │ 测试验证    │    │ 逐项实现    │    │
│   └─────────────┘    └─────────────┘    └──────┬──────┘    │
│                                                 │           │
│                                                 ▼           │
│                                         ┌─────────────┐    │
│                                         │ Verify      │    │
│                                         │ 全量验证    │    │
│                                         └─────────────┘    │
│                                                             │
│   模型分层：Opus(想清楚) → DeepSeek(写出来) → Sonnet(调好)  │
└─────────────────────────────────────────────────────────────┘
```

**核心原则**：
1. **先想后做**：Plan Mode + CLAUDE.md 确保方向正确
2. **小步快跑**：Propose-Apply-Verify 每次只做一个小变更
3. **自动验证**：Hooks + pytest 确保每次变更都经过测试
4. **可回滚**：Git checkpoint 确保出问题时能快速恢复
5. **成本可控**：模型分层策略在质量和成本之间找到平衡

---

## 三、关键 AI 协作案例深度剖析

### 案例 1：Anthropic 模型适配器 — 架构模式决策

#### 背景与问题

用户通过中转站（micuapi.ai）访问 Anthropic Claude 模型（`claude-sonnet-4-6`），但中转站对 `claude-` 前缀强制使用 Anthropic 原生 Messages API 协议，而非 OpenAI 标准的 Chat Completions 协议。直接使用 OpenAI SDK 发起请求返回 HTTP 400。

**核心约束**：路由层（`routes/api.py`）已为 OpenAI 接口编写并稳定运行，**不能修改**。

#### AI 协作过程

**第一阶段 — 问题诊断（学生主导）**：向 Claude Code 描述错误现象，AI 分析出中转站的协议路由逻辑（根据模型名前缀选择后端协议）。

**第二阶段 — 方案设计（学生决策，AI 辅助细化）**：AI 最初建议在 `routes/api.py` 中增加 `if "claude" in model:` 条件分支。学生**拒绝了此方案**，理由：违反开闭原则（OCP）——每次新增模型协议都需要修改路由层代码。学生提出采用 **Adapter 模式**，创建独立的 `AnthropicAdapter` 类，对外暴露与 OpenAI SDK 完全一致的 `client.chat.completions.create()` 接口。

**第三阶段 — 实现与打磨（AI 生成骨架，学生审核调整）**：AI 生成了 Adapter 类的核心代码，学生进行了以下关键修改：

1. **Temperature clamp 逻辑（学生独立添加）**：Anthropic API 的 temperature 范围是 `[0, 1]`，OpenAI 是 `[0, 2]`。在 `_Completions.create()` 中添加 `max(0.0, min(float(temperature), 1.0))` 防止参数越界。
2. **懒导入优化（学生独立设计）**：`import anthropic` 放在 `__init__()` 内部，确保不使用 Claude 时不会因缺少 `anthropic` 包而报错。
3. **流式/非流式双模式验证（学生手动测试）**：通过前端聊天界面和直接 API 调用分别验证两种模式。
4. **响应包装类的字段完整性（学生审查补齐）**：确保 `_Response`、`_StreamChunk`、`_Delta`、`_Choice` 的所有属性访问路径与 OpenAI SDK 响应对象完全一致。

#### 工程思维体现

- **SOLID 原则应用**：开闭原则（对扩展开放、对修改关闭）— Adapter 模式使路由层无需感知协议差异；单一职责原则（SRP）— Adapter 的职责仅限于协议转换。
- **设计模式选择**：在"条件分支"和"适配器模式"之间做出了正确的工程取舍。
- **防御性编程**：Temperature clamp 防止了运行时参数越界错误；懒导入防止了缺失依赖报错。

---

### 案例 2：Plotly 5.x 二进制编码问题 — 深度 Debug 全流程

#### 背景与问题

Amazon BestSelling Books 数据集上提问"画 Rank 和 Price 的散点图"后，图表只显示空白坐标轴和背景，没有任何数据点。控制台报错 `Object of type ndarray is not JSON serializable`。

这是本项目中最复杂、最具代表性的 Debug 案例，充分体现了**系统性排错思维**和**对 AI 局限性的认知**。

#### Debug 全流程（学生主导）

**Step 1 — 现象确认**
- 图表渲染空白（坐标轴存在但无数据点）
- 控制台报 ndarray 序列化错误
- 其他图表类型（柱状图、折线图）正常

**Step 2 — AI 初步尝试（失败）**
AI 建议用 `fig.to_json()` + `json.loads()` 替代 `fig.to_plotly_json()`。应用后问题依旧。

**Step 3 — 系统性诊断（学生主导，关键转折点）**
学生判断 AI 的建议停留在表面，决定深入服务端：

```python
# 学生在服务端添加诊断代码
print(type(x_data), x_data[:5] if hasattr(x_data, '__getitem__') else x_data)
```

输出揭示关键线索：
```json
{"dtype": "f8", "bdata": "AAAAAACAv0A..."}
```

而非预期的 `[1.0, 2.0, 3.0, ...]`。

**Step 4 — 根因定位（学生独立完成）**
学生阅读 Plotly 5.x 变更日志后确认：这是 **Plotly 5.0 引入的二进制编码特性**——`to_dict()` 和 `to_json()` 将 numpy 数组序列化为 Base64 编码以减小体积。服务端 Plotly 为 5.x，而前端 CDN 加载的 Plotly.js 为旧版本，版本不匹配导致解码失败。

**Step 5 — 解决方案设计（学生设计，AI 辅助生成代码）**
学生设计了递归解码器的完整方案，向 AI 描述需求后由 AI 生成代码：

- `_decode_plotly_bdata(obj)`：递归遍历整个 Figure dict，检测 `{dtype, bdata}` 模式并用 `base64.b64decode` + `numpy.frombuffer` 解码
- `_plotly_fig_to_dict(fig)`：统一序列化入口，`fig.to_dict()` → `_decode_plotly_bdata()` 的完整链路
- `_json_serial(obj)`：处理 ndarray → list 转换，解决 JSON 序列化问题

**Step 6 — 多重修复验证**
1. 散点图数据点正确显示 ✓
2. 发现 `_ensureMarkerVisibility()` 中 `Object.assign` 参数顺序错误（空对象在前覆盖了配置），修复为 `Object.assign(defaultMarker, trace.marker || {}, ...)`
3. 前端 `_cleanChartLayout()` 剥离 plotly_dark 模板（模板的黑色背景 + 黑色 marker 导致点不可见）

#### 工程思维体现

- **系统性排错**：没有停留在 AI 的第一次建议，而是自建诊断体系深入根因
- **版本兼容性意识**：识别出服务端/前端版本不匹配是根本原因，而非代码逻辑错误
- **全链路思维**：从服务端序列化 → JSON 传输 → 前端反序列化 → 渲染整条数据链路排查
- **方案完整性**：不仅修复了当前图表类型，设计了递归解码器理论上兼容所有 Plotly 图表

---

### 案例 3：代码安全沙箱 — 多层防护体系及安全审计

#### 背景与问题

系统核心功能是让 AI 生成 Python 代码并通过 `exec()` 在服务端执行。必须在**不影响正常数据分析功能**的前提下建立可靠的安全防护。

#### AI 协作过程

AI 提出了基础的多层防护方案：黑名单关键词 → 受限内置函数 → 代码净化 → 受限命名空间 → 线程超时。

但学生在审查中发现了 **3 个关键安全漏洞**：

#### 漏洞 1：防护顺序错误（学生独立发现，高危）

**AI 原始代码：**
```python
code = _sanitize_code(code)      # 先净化（移除 import 行）
if not self.validate_code(code):  # 再校验
    return error
```

**漏洞分析**：净化会移除 `import os` 等 import 语句，但不会移除 `subprocess.call(...)`、`open("/etc/passwd")` 等非 import 的危险操作。攻击者可将危险代码放在非 import 行中绕过检测。

**学生修复**：
```python
if not self.validate_code(code):  # 先校验原始代码
    return error
code = _sanitize_code(code)      # 再净化
```

这是本项目中发现的最严重的安全漏洞。它说明了 **AI 不了解代码执行流程中的攻击向量**——AI 看到了"需要校验"和"需要净化"两个需求，但没有意识到它们的执行顺序构成了安全边界。

#### 漏洞 2：黑名单不完整（学生逐项审计）

**AI 原始黑名单**：`import os`、`import sys`、`subprocess`、`__import__`、`eval(`、`exec(`、`open(`

**学生补充**（逐项审查后）：
- `shutil`：文件系统操作（复制、删除、移动）
- `pathlib`：路径遍历和信息收集
- `socket`：网络连接和反向 shell
- `globals(`：命名空间逃逸
- `locals(`：符号表访问
- `compile(`：动态代码编译绕过
- `breakpoint(`：调试器注入

#### 漏洞 3：代码净化不够彻底

**AI 原始实现**：仅匹配 `import pandas` / `import numpy` / `import plotly` 三种模式。

**学生修复**：升级为全量正则 `^\s*(import|from)\s+\S+`，移除**所有** import 语句。理由：无法预知攻击者会 import 什么模块，必须全部移除。

#### 工程思维体现

- **安全审计能力**：不满足于"AI 说这个安全"，而是逐项审查每一条规则
- **攻击面思维**：从攻击者视角思考如何绕过防护（"如果我把危险代码放在 import 行之外会怎样？"）
- **纵深防御**：6 层防护不是简单的叠加，而是精心排序形成防线链

---

### 案例 4：全数据集通用化改造 — 系统架构升级

#### 背景与问题

系统最初围绕 Online Retail 零售交易数据集设计。切换其他类型数据集（书籍评分、纯分类调查数据、时间序列数据）时出现图表空白、分析错误、建议问题不适用等问题。

#### 架构设计过程（学生主导）

**Step 1 — 问题域分析**

学生识别出系统中的**硬编码零售业务逻辑**：
- 列名假设（`InvoiceDate`、`Country`、`StockCode`、`UnitPrice`）
- 分析假设（有销售额列、有客户列、有日期列）
- 图表假设（6 个预置图表全部针对零售场景）
- 建议问题假设（"月均销售额是多少？"）

**Step 2 — 画像分类体系设计（学生独立设计）**

AI 最初提议 4 种画像。学生基于数据科学实践经验扩展为 **6 种**：

| 画像 | 识别规则 | 数据科学依据 |
|------|---------|-------------|
| `retail` | 日期 + 客户 + 商品/金额列 | 交易型数据，适用 RFM、漏斗分析 |
| `temporal` | 日期列 + 数值列 | 时间序列，适用趋势/季节分解 |
| `numeric` | 数值列 ≥ 4，分类列 ≤ 2 | 科学/测量数据，适用分布/相关分析 |
| `categorical` | 分类列 ≥ 3 | 调查/问卷数据，适用频次/交叉分析 |
| `geographic` | 国家/城市/省份列 | 空间数据，适用地图/地区对比 |
| `mixed` | 其余 | 通用兜底 |

**Step 3 — 纯分类数据降级策略（学生独立设计）**

AI 的方案中，纯分类数据集（如调查问卷）会因为没有数值列而生成空白图表卡片。学生设计了降级策略：当检测到分类型画像且无数值列时，自动将散点图和分布图替换为频次柱状图。

**Step 4 — 11 个子任务的分步实现**

学生设计了完整的实现顺序，使用 git 逐任务提交（12 个 commits）：

```
DataProfiler 画像检测 → 列信息注入 → 自适应图表配置 
→ Analyzer 通用方法 → API 端点 → ChatSession 语义感知 
→ 前端自适应渲染 → 快捷提问动态生成 → 测试回归验证
```

每完成一个子任务都运行全量测试（276 → 338 测试），确保零回归。

#### 工程思维体现

- **领域建模**：将"数据类型"这一模糊概念形式化为 6 种可检测、可路由的画像
- **降级设计**：纯分类数据不是"不支持"而是"自适应降级"，保证所有数据类型都有有意义的输出
- **增量交付**：11 个子任务独立提交，每个都可验证，降低集成风险
- **关注点分离**：画像检测（profiler.py）与应用层（routes/api.py）通过标准化数据接口通信

---

### 案例 5：技能化问数架构 — 从"代码生成"到"技能路由"

#### 背景与问题

v3.1 的智能问答完全依赖 LLM 生成 Python 代码并通过沙箱执行。这种"代码生成兜底"模式存在三个固有问题：

1. **不确定性**：同一问题两次调用可能生成不同的代码，得到不同的结果
2. **延迟高**：LLM 生成代码 + 沙箱执行 + 结果解释，三次串行等待
3. **幻觉风险**：LLM 可能生成语法正确的代码但执行错误的分析逻辑（如用错聚合函数、算错统计量）

#### 架构重构设计（学生主导）

学生提出**"技能优先路由"**架构——将常见分析操作封装为确定性 Python 脚本，LLM 仅负责"选择技能"和"解释结果"，不负责"生成分析逻辑"。

```
v3.1（旧）：用户问题 → LLM 生成代码 → 沙箱执行 → 返回结果

v3.2（新）：用户问题 → LLM 选择技能 + JSON计划 → 确定性脚本执行 
           → 证据表 → LLM 基于证据流式解释 → 返回结果
           ↘ 无匹配 → 代码生成兜底（零改动兼容）
```

**6 个确定性技能**：

| 技能 | 覆盖的分析操作 | 确定性保证 |
|------|-------------|-----------|
| `stats-skill` | 均值/中位数/方差/分组统计 | pandas 内置统计方法 |
| `viz-skill` | 柱/折/散/饼图 | plotly 标准图表类型 |
| `trend-skill` | 时间序列重采样趋势 | pandas resample 标准语义 |
| `correlation-skill` | 相关矩阵 + 热力图 | numpy corrcoef 标准算法 |
| `distribution-skill` | 直方图/箱线图 | numpy histogram + matplotlib |
| `profile-skill` | 概览/schema/质量画像 | DataProfiler 确定性规则 |

**双阶段 LLM 调用**：

- **第一阶段**（技能选择）：LLM 输入用户问题 + 数据集 schema → 输出 `{skill, plan, reason}` JSON
- **第二阶段**（证据解释）：LLM 输入用户问题 + 证据表（脚本执行的实际结果）→ 流式输出中文解释

**关键设计决策**：
- 技能选择由 LLM 完成（需要语义理解），但分析执行由脚本完成（需要确定性）
- 证据先于解释：LLM 生成解释时已拿到实际数据，杜绝"编造分析结果"的幻觉
- 代码生成兜底保留：未命中任何技能时降级到原有的代码生成路径，行为与 v3.1 一致

#### 工程思维体现

- **范式转换**：从"生成式"到"路由式"，根本性地解决了分析确定性问题
- **职责分离**：LLM 负责语义理解（选技能 + 解释），脚本负责数学计算，边界清晰
- **向后兼容**：代码生成兜底路径零改动保留，确保升级无破坏
- **契约设计**：`SkillResult` 数据类 + 公共校验函数，6 个技能共享统一的输入输出契约

---

### 案例 6：数据预处理可视化 — 全链路数据追踪

#### 背景与问题

数据概览页面的"清洗前后数据量对比图"显示范围为 -1 到 1，所有数据点为 0，视觉上完全空白。

#### 排查过程（学生主导）

AI 最初判断为图表库配置问题。学生决定**直接阅读后端源码**：

1. 阅读 `data/preprocessor.py` 的 `run_all()` 方法，确认返回结构
2. 发现代码中使用 `pp_report.get("duplicates_removed")` 读取去重数据
3. 但实际返回结构中该数据嵌套在 `pp_report["remove_duplicates"]["removed"]` 路径下
4. AI **假设了键名而没有验证**——这是典型的"AI 幻觉"在工程中的表现

**学生修复**：
- 定位 `pp_report` 的完整嵌套结构（`remove_duplicates.removed`、`filter_invalid_records.removed`、`handle_missing`）
- 设计了 Pipeline 数据重构算法：从 `rows_final` 反推 `rows_after_inv` 和 `rows_orig`
- 将朴素的"清洗前后对比"升级为**三阶段流水线可视化**：原始数据 → 去重后 → 无效过滤后
- 同步修改 4 个文件（`data/analyzer.py`、`routes/api.py`、`templates/index.html`、测试文件）

#### 工程思维体现

- **根因分析**：不满足于修复表面现象，追溯到数据结构的根本问题
- **数据完整性**：Pipeline 可视化展示数据在每一步的真实变化量，而非抽象百分比
- **跨模块协调**：后端数据生成 → API 端点 → 前端容器 → 测试，4 个文件同步修改

---

## 四、工程师思维体现

### 3.1 SOLID 原则的系统性应用

| 原则 | 在本项目中的具体体现 |
|------|-------------------|
| **S — 单一职责** | 每个 `.py` 文件只做一件事：`loader.py` 只管读取，`preprocessor.py` 只管清洗，`code_generator.py` 只管沙箱执行。`AnthropicAdapter` 的职责仅限于协议转换，不涉及其它逻辑。 |
| **O — 开闭原则** | Adapter 模式使新增模型支持时无需修改路由层；技能注册表 (`skills/__init__.py`) 使新增分析技能时只需添加目录和脚本，无需改动 `SkillRouter`。 |
| **L — 里氏替换** | `AnthropicAdapter` 与 OpenAI SDK 客户端完全可替换——调用方通过 `client.chat.completions.create()` 接口无法区分底层是 OpenAI 还是 Anthropic。 |
| **I — 接口隔离** | 技能契约层 (`SkillResult` + `validate_result`) 是最小化接口——脚本只需返回 `{type, data, metadata}` 结构的 dict，不暴露实现细节。 |
| **D — 依赖抽象** | AI 层通过 `_init_ai_client()` 工厂函数返回统一的 `client` 对象，上层代码依赖抽象接口而非具体 SDK。 |

### 3.2 防御性编程

- **宽数据集截断**：>25 列时系统提示词仅展示前 25 列，防止 token 超限
- **SSE 连接 120s 全局超时**：`AbortController.abort()` 防止僵尸连接
- **JSON 解析容错**：`_parse_json()` 支持 trailing comma、非标准引号，失败后 fallback 到规则引擎
- **优雅降级全覆盖**：7 个依赖 AI 的功能全部内置 fallback 路径

### 3.3 可测试性设计

- 所有外部依赖（OpenAI API、文件系统）均通过依赖注入或 Mock 隔离
- 338 个测试用例全部使用 `MagicMock`，无需真实 API Key
- 测试覆盖包括正常路径、边界条件、异常路径、安全沙箱
- SSE 流式响应测试验证事件类型完整性和格式正确性

### 3.4 版本控制纪律

- 85+ 个原子化 commits，每个 commit 对应单一逻辑变更
- Commit message 遵循 Conventional Commits 规范（`feat:` / `fix:` / `docs:` / `test:` / `chore:`）
- 功能开发和 Bug 修复分离提交，便于回溯和 cherry-pick

---

## 五、设计思维体现

### 4.1 架构设计 — 三层分离 + 关注点隔离

```
routes/（路由层）  →  仅处理 HTTP 请求/响应，不包含业务逻辑
   ai/（AI 层）    →  仅处理 LLM 交互和智能决策，不直接操作数据
 data/（数据层）   →  仅处理数据 I/O 和计算，不依赖 Web 框架
```

这种三层分离是在项目启动时由学生**独立设计**的架构决策。AI 在每层内部提供了实现帮助，但层与层之间的职责边界和数据流向由学生定义。

### 4.2 交互设计 — SSE 事件驱动架构

传统 SSE 仅支持 GET 请求，无法携带 JSON body。学生设计了基于 `fetch` + `ReadableStream` 的 **POST SSE** 模式：

- **事件类型系统**：`route → evidence → text_delta ×N → chart(可选) → done → [DONE]`
- **事件驱动路由**：`dispatch()` 按 `type` 字段分发到独立 handler，调用方无需关心 SSE 协议细节
- **跨 chunk 缓冲区拼接**：未完成行自动保留至下一 chunk，防止 JSON 截断
- **AbortController 生命周期管理**：精确控制单连接取消，`_sendingLock` 防抖并发

这一设计在项目初期由学生完成架构规划，AI 辅助实现了具体代码。

### 4.3 视觉设计 — 赛博朋克沉浸式体验

- **多层次背景系统**：粒子星场（Canvas 动画）+ 星云光晕 + 电路网格 + 全息 3D 旋转立方体 + 赛博视频背景（mix-blend-mode: screen）
- **液态玻璃卡片**：纯 CSS `backdrop-filter: blur()` + `::before` 渐变遮罩边框
- **字体系统**：Instrument Serif（标题衬线）+ Barlow（正文）+ JetBrains Mono（代码），三层字体语义分离
- **z-index 层级管理**：0（背景特效）→ 1（视频背景）→ 2（主内容区）→ 100（导航栏）→ 9990（拖拽遮罩）

### 4.4 降级设计 — AI Enhanced, Not AI Dependent

核心设计哲学贯穿整个系统：**AI 是加速器，不是单点故障源**。7 个依赖 AI 的功能全部配备独立的规则引擎 fallback，确保无 API Key 时核心体验完整。

---

## 六、产品思维体现

### 5.1 用户分层与降低使用门槛

- **零配置体验**：不上传 API Key 也能使用 80% 的核心功能（数据上传、清洗、可视化、质量评分、洞察、报告模板）
- **快捷提问按钮**：基于数据画像动态生成 4 条专属问题，用户无需自己想问题
- **拖拽上传**：全局拖拽监听，任意位置松手即上传，不需要找到"上传按钮"

### 5.2 渐进式信息披露

- **洞察面板**：上传数据后**主动推送**洞察卡片，而非等待用户提问
- **预处理可视化**：7 步 Pipeline 以流程图形式展示，每步标注处理行数
- **图表工作台**：快捷操作按钮（"改折线图""按季度聚合""只看 Top 5"）降低图表修改门槛

### 5.3 多服务商支持 — 降低用户的供应商锁定风险

支持 8 种服务商（OpenAI / DeepSeek / Kimi / 智谱 / 通义千问 / 硅基流动 / 豆包 / Ollama 本地），运行时一键切换无需重启。这不是技术炫技，而是**产品决策**——让用户选择自己信任和负担得起的服务商。

### 5.4 面向非技术用户的错误处理

- 不暴露 Python Traceback，显示友好的中文提示
- 图表空白时显示"暂无数据：数据集缺少对应列"而非静默失败
- AI 配置面板内置"测试连接"按钮 + Base URL 格式自动校验

---

## 七、AI 能力边界深度思考

### 6.1 AI 擅长什么：模式匹配与模板填充

经过本项目 5 周的密集使用，我对 AI 的能力边界有了清晰的认识：

**AI 的强项领域：**

| 能力 | 本质 | 本项目中的例子 |
|------|------|-------------|
| 代码骨架生成 | 模式识别 + 模板填充 | DataProfiler 的 6 个检测方法骨架、测试文件的结构 |
| API 使用指导 | 训练数据中包含海量文档 | `pandas.DataFrame.resample` 的参数和用法 |
| 重复性编码 | 高度模式化的任务 | 序列化/反序列化函数、CRUD API 端点 |
| 代码解释 | 模式匹配 + 知识检索 | Plotly 5.x 二进制编码机制的讲解 |
| 文本组织 | 语言模型的核心能力 | 报告润色、叙事文本生成 |

**共性特征**：这些任务都可以归纳为"给定明确的输入格式和输出规格，生成符合模式的代码或文本"。

### 6.2 AI 不擅长什么：需要上下文、判断和创造力的任务

| 局限性 | 根本原因 | 本项目中的实际案例 |
|--------|---------|-----------------|
| **缺乏项目全局上下文** | 上下文窗口限制 + 无持久记忆 | AI 建议在路由层直接分支而不是用 Adapter 模式——它不了解路由层的稳定性和扩展性约束 |
| **版本兼容性盲区** | 训练数据覆盖多版本，不能感知当前环境 | Plotly 5.x 二进制编码与旧版 Plotly.js 的不兼容——AI 不知道前端 CDN 加载的是什么版本 |
| **幻觉** | 概率生成模型的固有属性 | `pp_report.get("duplicates_removed")`——AI 猜测了不存在的 JSON 键名 |
| **被动响应** | 无自主意识，不会主动测试 | 所有 20+ Bug 都是学生在测试中发现后报告给 AI 的，AI 从未主动发现任何运行时 Bug |
| **安全盲点** | 训练目标是最佳猜测，非安全分析 | validate/sanitize 顺序漏洞——AI 理解两个函数各自的作用，但不理解顺序构成了安全边界 |
| **架构判断** | 无"品味"和"经验直觉" | AI 无法判断何时该用单体、何时该拆分、何时该引入抽象层 |

### 6.3 核心洞察："已知模式" vs "未知边界"

这是我在本项目中形成的最重要的认知框架：

```
┌────────────────────────────────────────────────────┐
│                   AI 的能力空间                       │
│                                                    │
│   ┌──────────────────────┐                         │
│   │    "已知模式"空间     │  ← AI 表现优秀           │
│   │  - 代码骨架生成       │                         │
│   │  - API 调用模板       │    边界由训练数据         │
│   │  - 单元测试编写       │    的覆盖范围决定         │
│   │  - 文档查询          │                         │
│   └──────────────────────┘                         │
│                                                    │
│   ┌──────────────────────┐                         │
│   │    "未知边界"空间     │  ← AI 表现显著受限        │
│   │  - 版本兼容性 Bug     │                         │
│   │  - 安全漏洞审计       │    这些是需要人类          │
│   │  - 架构设计决策       │    判断力的领域            │
│   │  - 运行时错误根因     │                         │
│   │  - 跨模块影响分析     │                         │
│   └──────────────────────┘                         │
│                                                    │
└────────────────────────────────────────────────────┘
```

**结论**：AI 在本项目中的角色更接近于一个能力极强的"高级工程师"——在明确的设计规范下能高效产出代码——而非"架构师"或"QA"。战略性的架构决策、系统性的 Debug 思路、安全质量的终极把关，始终需要人类开发者来主导。

### 6.4 AI 不会做的事（在我要求之前）

- **主动测试**：AI 生成代码后不会主动运行测试验证
- **审查自己的代码**：AI 不会发现自己代码中的安全漏洞或逻辑错误
- **考虑跨模块影响**：修改 `analyzer.py` 时不会检查 `api.py` 是否受影响
- **主动发现 Bug**：AI 不会主动浏览代码库寻找潜在问题
- **质疑用户需求**：当用户的需求在技术上不合理时，AI 不会主动建议替代方案

### 6.5 对"AI 替代程序员"论调的回应

基于本项目 5 周的深度 AI 协作经验，我的观点是：

**AI 替代的是"写代码"这个动作，而非"做工程"这个能力。** 真正困难的从来不是敲代码——而是知道需要写什么代码、为什么这样写、写完后如何验证它是对的、以及当它出错时如何找到问题。

本项目 20+ Bug 的发现、8 个被拒绝的 AI 方案、15+ 项独立架构决策，每一项都指向同一个结论：**最具创造性和最具风险的部分，始终需要人类来完成。**

---

## 八、Bug 发现与修复全记录

以下列出本项目中学生发现并修复的全部重要 Bug。**所有 Bug 均由学生在测试和使用中发现**，AI 在收到具体错误信息后辅助修复。

### 7.1 安全类 Bug（高危）

| # | Bug | 发现方式 | 根因 | 修复 |
|---|-----|---------|------|------|
| 1 | `validate_code` 与 `_sanitize_code` 执行顺序错误 | 代码审查 | 净化后校验，攻击者可绕过黑名单 | 先校验原始代码，再净化 |
| 2 | `_FORBIDDEN` 黑名单不完整 | 逐项审计 | AI 生成的初始列表遗漏 6 个高危关键词 | 补充 shutil/pathlib/socket/globals/locals/compile/breakpoint |
| 3 | 代码净化仅匹配 3 种 import 模式 | 代码审查 | 正则表达式范围过窄 | 升级为全量 `^\s*(import\|from)\s+\S+` |

### 7.2 渲染类 Bug

| # | Bug | 发现方式 | 根因 | 修复 |
|---|-----|---------|------|------|
| 4 | 散点图空白（数据点不可见） | 前端测试 | ① Plotly 5.x 二进制编码 ② plotly_dark 模板黑色 marker + 黑色背景 | `_decode_plotly_bdata()` 递归解码 + `_cleanChartLayout()` 剥离模板 |
| 5 | 图表工作台→聊天同步后图表消失 | 前端测试 | `let` 变量作用域问题 + 旧 SSE 连接竞态 | `let`→`window` 全局变量 + `_activeSSEConnection.abort()` |
| 6 | 仪表盘图表尺寸错误 | 前端测试 | flex 容器样式残留 | 渲染前重置 flex 相关样式 + 双重 resize |
| 7 | 时效性进度条不显示 | 前端测试 | CSS 变量名错误：`var(--yellow)` 不存在 | 改为 `var(--amber)` |
| 8 | 预处理可视化图表范围 (-1, 1) 全为 0 | 前端测试 | 嵌套 JSON 键名假设错误 | 阅读源码定位正确键路径 + Pipeline 数据重构算法 |
| 9 | 缺失率热力图溢出容器 | 前端测试 | CSS 布局问题 | 动态高度计算 + overflow:hidden |

### 7.3 数据处理类 Bug

| # | Bug | 发现方式 | 根因 | 修复 |
|---|-----|---------|------|------|
| 10 | 非逗号分隔 CSV 解析失败 | 多数据集测试 | 未检测分隔符 | csv.Sniffer 自动嗅探 |
| 11 | 纯分类数据集图表全部空白 | 多数据集测试 | 无数值列但按数值列生成图表 | 自适应降级：全切换为频次柱状图 |
| 12 | 宽数据集（100+列）AI 提示词超长 | 边界测试 | 系统提示词包含全部列信息 | >25列截断 + 保留前3样本值 |
| 13 | 未来日期显示"数据距今 -120 天" | 边界测试 | `days_ago < 0` 时公式产生负数 | 直接返回满分 + 友好文案 |

### 7.4 交互/同步类 Bug

| # | Bug | 发现方式 | 根因 | 修复 |
|---|-----|---------|------|------|
| 14 | 图表工作台→聊天方向同步失效 | 前端测试 | `updateChatChartFromWorkspace` 未暴露为全局函数 | `window.updateChatChartFromWorkspace` |
| 15 | 快捷修改后工作台全局状态丢失 | 前端测试 | `Object.assign` 参数顺序错误 | 修正合并顺序 |
| 16 | SSE 流式响应跨 chunk JSON 截断 | 边界测试 | 未处理跨 chunk 的不完整行 | buffer 残留机制 + chunk 拼接重试 |
| 17 | 并发发送消息导致 SSE 事件错乱 | 压力测试 | 无并发控制 | `_sendingLock` 布尔锁 |

### 7.5 数据分析类 Bug

| # | Bug | 发现方式 | 根因 | 修复 |
|---|-----|---------|------|------|
| 18 | 建议问题与数据集无关 | 多数据集测试 | 固定模板问题 | 基于数据画像 + 实际列名动态生成 |
| 19 | AI 生成代码中写 `import plotly.express` 报错 | 功能测试 | AI 不知道 px 已预加载 | 系统提示词明确告知"禁止 import 语句" |
| 20 | ndarray 类型 JSON 序列化失败 | 功能测试 | Flask jsonify 不支持 numpy 类型 | `_json_serial()` 递归转换 numpy→Python 原生类型 |

---

## 九、学生独立决策与贡献清单

以下 15 项关键决策由学生独立做出，塑造了项目的架构、质量和功能完整性：

### 架构决策（5 项）

| # | 决策 | 替代方案（AI 建议或常见做法） | 决策理由 |
|---|------|---------------------------|---------|
| 1 | Adapter 模式解耦模型接入 | AI 建议在路由层加条件分支 | 开闭原则：新增模型时无需修改路由层 |
| 2 | 三层分离架构（routes/ai/data） | 无（AI 不会主动规划目录结构） | 关注点隔离，每层可独立测试和替换 |
| 3 | 技能优先路由重构 | AI 未提出此方向 | 根本性解决分析确定性和幻觉问题 |
| 4 | 单体 Flask 架构（放弃微服务） | 行业流行微服务 | YAGNI：个人项目无需引入容器/消息队列 |
| 5 | SSE POST-body 流式底座 | 行业标准 SSE 仅支持 GET | 需要在 POST body 中携带完整问题上下文 |

### 安全决策（3 项）

| # | 决策 | 发现方式 | 影响 |
|---|------|---------|------|
| 6 | validate→sanitize 防护顺序 | 代码审查中发现逻辑漏洞 | 防止高危安全漏洞进入生产环境 |
| 7 | `_FORBIDDEN` 黑名单审计与补全 | 逐项审查每条规则 | 补齐 6 个遗漏的高危关键词 |
| 8 | 全量 import 净化正则 | 审查发现仅匹配 3 种 import | 消除未知模块导入的潜在攻击面 |

### 数据科学决策（4 项）

| # | 决策 | 实现 | 影响 |
|---|------|------|------|
| 9 | 6 种数据画像分类体系 | DataProfiler | 使系统从"零售专用"升级为"通用分析平台" |
| 10 | 纯分类数据自适应降级 | api.py 降级逻辑 | 无数值列的数据集不会出现空白图表卡片 |
| 11 | 洞察阈值设定 | insight.py 阈值调优 | 平衡了洞察的灵敏度和噪声 |
| 12 | 时效性维度的未来日期处理 | quality_scorer.py 边界处理 | 避免出现"数据距今 -120 天"的荒谬输出 |

### 产品决策（3 项）

| # | 决策 | 实现 | 影响 |
|---|------|------|------|
| 13 | 8 种服务商支持 + 界面一键切换 | config 面板 | 用户不被锁定在单一服务商 |
| 14 | Ollama 本地免费部署支持 | 自动填入 Base URL + API Key | 零成本使用全部 AI 功能 |
| 15 | 优雅降级全覆盖 | 7 个功能的独立 fallback | 无 API Key 时核心体验 80% 可用 |

---

## 十、AI 生成内容中被拒绝/修正的部分

### 9.1 架构层面 — 被拒绝的方案

**AI 建议 1**：在 `routes/api.py` 的 `_init_ai_client()` 中用 `if "claude" in model:` 分支处理不同协议。

**拒绝原因**：违反开闭原则。每次新增模型协议都需修改路由层。学生选择了 Adapter 模式，通过工厂函数自动路由。

**AI 建议 2**：对宽数据集只列出列名，不包含数据类型和样本值。

**拒绝原因**：样本值对 AI 理解列语义至关重要。`CitricAcid` 列的样本值 `[0.49, 0.0, 0.32]` 远比列名本身更能帮助 AI 判断这是数值特征。学生决定保留数据类型和样本值，仅截断列数。

### 9.2 代码层面 — 被修正的实现

**AI 生成 — `_sanitize_code` 位置错误**（详见案例 3）：AI 将净化放在校验之前，构成了安全漏洞。

**AI 生成 — `_FORBIDDEN` 列表不完整**（详见案例 3）：AI 遗漏了 shutil、pathlib、socket 等 6 个高危关键词。

**AI 生成 — `preprocess_visual` 键名错误**（详见案例 6）：AI 假设了 `pp_report` 的键名结构而未查看实际返回格式。

**AI 生成 — `Object.assign` 参数顺序错误**：`Object.assign({}, defaultMarker, trace.marker)` 将空对象放在首位，覆盖了后续配置。应改为 `Object.assign(defaultMarker, trace.marker || {}, ...)`。

**AI 生成 — 过度设计的图表配置**：`_ensureMarkerVisibility()` 函数试图通过复杂的深拷贝策略覆盖所有 marker 属性。学生简化为分层合并策略。

### 9.3 设计层面 — 被优化的方案

**AI 提议 4 种画像 → 学生扩展为 6 种**：AI 的类型学分类过于粗糙，学生在数据科学实践中识别出需要更细粒度的分类。

**AI 的固定模板建议问题 → 学生改为基于画像+列名动态生成**：AI 没有意识到"月均销售额是多少？"在非零售数据集上的荒谬性。

**AI 的 1000 tokens 叙事限制 → 学生提升至 3000**：AI 没有意识到 1000 tokens 不足以完成完整的叙事弧线。

---

## 十一、AI 辅助编程价值与风险反思

### 10.1 效率提升量化

| 开发活动 | 传统方式估计 | AI 辅助实际 | 效率提升 |
|----------|:----------:|:----------:|:-------:|
| DataProfiler 代码骨架 | 3-4 小时 | 30 分钟（AI）+ 1 小时（审查） | ~70% |
| 20 个测试文件编写 | 8-10 小时 | 3 小时（AI）+ 2 小时（审查） | ~50% |
| Plotly 二进制编码 Debug | 4-6 小时 | 2 小时（诊断）+ 1 小时（修复） | ~50% |
| Adapter 模式实现 | 3-4 小时 | 45 分钟（AI）+ 30 分钟（调整） | ~70% |
| 5 个 API 端点新增 | 6-8 小时 | 2 小时（AI）+ 2 小时（调优） | ~50% |
| 技能层 6 个技能脚本 | 6-8 小时 | 3 小时（AI）+ 2 小时（审查） | ~45% |

**总体估算**：AI 辅助编程使开发效率提升约 **50-70%**。节省的时间主要来自减少重复性编码（测试、序列化、错误处理）和加速代码骨架搭建。但 Debug、架构设计、安全审计的时间并未显著减少——这些恰恰是最需要人类判断力的环节。

### 10.2 质量保障

**"AI 生成的代码能用，但不能直接用"**——这是我在本项目中的核心经验。

- AI 生成了约 70% 的测试代码，但学生审查了**每一个测试用例的断言逻辑**
- 本项目中发现的多处关键 Bug（安全防护顺序、键名错误、Object.assign 参数顺序）全部由学生审查发现
- AI 生成的代码在"正常路径"上通常正确，但在"异常路径"（边界条件、错误处理、安全防护）上需要大量人工修正

### 10.3 学习价值

- **通过 AI 解释加深理解**：Plotly 5.x 编码机制、Anthropic Messages API 协议差异——AI 的即时解释帮助快速理解陌生领域
- **通过讨论锻炼架构思维**：与 AI 讨论 Adapter vs 条件分支方案时，需要清晰表达架构取舍的理由
- **通过审查提升代码品味**：持续审查 AI 代码养成批判性阅读习惯，学会识别"能工作但不优雅"的代码

### 10.4 关键风险

1. **过度依赖风险**：不审查直接使用 AI 代码，本项目至少会有 4 处关键 Bug 进入代码库
2. **"AI 能写一切"的错觉**：AI 在已知模式内强，在未知边界弱。Plotly 5.x Debug 过程说明了真正困难的是"知道需要写什么代码"
3. **上下文窗口限制**：大型代码库无法一次加载。通过分模块、分子任务规避，每次聚焦单一模块
4. **AI 不会说"我不确定"**：AI 总是给出自信的回答，即使它在猜测。这要求开发者保持怀疑态度

### 10.5 最终认识

> **AI 辅助编程的最大价值在于将开发者从重复性编码中解放出来，使其能专注于更高层次的思考——架构设计、安全审查、用户体验、系统集成。但最具创造性和最具风险的部分（设计决策、安全把关、质量验证）始终需要人类来完成。**
>
> **AI 是强大的工具，但不是替代品。会用 AI 的工程师取代不会用 AI 的工程师——但不会思考的工程师，无论用不用 AI，都会被取代。**

---

## 附录：项目规模与测试覆盖

### 代码规模

| 目录 | 文件数 | 行数（估算） | 主要职责 |
|------|:-----:|:----------:|---------|
| `ai/` | 8 | ~1,600 | AI 交互：对话管理、代码沙箱、叙事引擎、技能路由、模型适配、多Agent报告 |
| `data/` | 6 | ~1,600 | 数据处理：加载、清洗、分析、画像、质量评分、洞察 |
| `routes/` | 2 | ~1,200 | API 路由：页面路由、RESTful 端点、SSE 流式组件 |
| `skills/` | 7 | ~1,000 | 确定性技能：stats/viz/trend/correlation/distribution/profile + 契约层 |
| `static/js/` | 6 | ~2,500 | 前端：SSE handler、图表工作台、聊天、仪表盘、洞察卡片 |
| `static/css/` | 1 | ~800 | 全量设计系统 |
| `templates/` | 5 | ~800 | Jinja2 HTML 模板 |
| `tests/` | 30 | ~3,500 | 测试套件：338 个测试用例，全 Mock |
| **总计** | **~65** | **~13,000** | — |

### 测试覆盖

- **测试文件数**：30 个
- **测试用例数**：338 个
- **全部使用 MagicMock**：零外部依赖，CI 环境可直接运行
- **覆盖的模块**：loader, preprocessor, analyzer, detector, chat, code_generator, insight, report, report_agents, chart_generator, plan_generator, storyteller, quality_scorer, profiler, preprocess_visualizer, skill_contract, skill_stats/viz/trend/correlation/distribution/profile, skill_registry, skill_router, adaptive_charts_contract, api

### 版本演进

| 版本 | 日期 | 测试数 | 主要里程碑 |
|------|------|:-----:|----------|
| v1.0 | 2026-05-17 | ~60 | 初始提交：五大核心模块 + 三大创新功能 |
| v2.0 | 2026-05-25 | ~180 | 多Agent报告 + 预处理增强 + Ollama支持 |
| v3.0 | 2026-05-25 | ~247 | SSE流式 + NL2Vis图表工作台 + 质量评分卡 + 叙事引擎 |
| v3.1 | 2026-05-26 | 276 | Bug修复批次（6个修复）+ 全数据集通用化改造 |
| v3.2 | 2026-06-02 | 338 | 技能化问数架构（6个技能 + SkillRouter） |

---

*本报告基于真实的项目开发经历撰写，所有案例均有对应的 git commit 记录可追溯（85+ commits，跨度 2026-05-17 至 2026-06-05）。项目源代码中的注释标注了"来源：学生"、"来源：AI"、"来源：学生+AI"以区分代码来源。*
